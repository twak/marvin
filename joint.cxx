/**
   joint.cxx

   This defines an IK joint item.
 */
#include "joint.h"

/**
   Rel is the position in the parent that we would be at!
 */

// joint::joint(joint * p, point *rel)
// {
//   parent = p;
//   if (parent != NULL)
//     {
//       parent -> addChild(this);
//     }
//   child  = NULL;


//   // set default angles as straight ahead
//   nAlpha = 0.0;
//   nTheta = 0.0;
//   parentPosition = new point(rel);
//   numHooks = 0;
// }

// joint::joint(point *p)
// {
//   printf("HI 0x%x",p);
// }

